# ParamSearch
A Chrome Extension that allows users to make google searches from a small window on the same tab. Extension implements Google's
parameter search in a more user friendly way to let users decide which phrases they want to include or exclude from their search, 
as well as choose the domains of the search results

# What I learned
UX (Custom buttons and search bars, hover actions, etc.)
